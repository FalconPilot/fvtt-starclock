<header class="item-header flexrow mgb">
  <img
    src="{{item.img}}"
    data-edit="img"
    title="{{item.name}}"
  />
  <h1>
    <input
      type="text"
      name="name"
      value="{{item.name}}"
      class="pgl line-input"
      placeholder="{{ localize 'SCLK.ItemName' }}"
    />
  </h1>
</header>
